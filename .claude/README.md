# .claude/

Project-scoped Claude Code config.

```
.claude/
  commands/   # slash commands (project-specific)
  rules/      # project rules
  skills/     # project skills
  hooks/      # PreToolUse/PostToolUse shell scripts
  plans/      # planning docs (gitignored — vidi .gitignore)
  settings.json
```

Plans su gitignored. AGENTS.md u root-u je kanonski izvor istine za AI.
